# encoding: UTF-8

FactoryGirl.define do
  factory :<%= nom_arch %>, class: '<%= nom_clase %>' do
    id 1000 # Buscamos que no interfiera con existentes
    nombre "<%= nom_clase %>"
    fechacreacion "<%= Date.today %>"
    created_at "<%= Date.today %>"
  end
end
