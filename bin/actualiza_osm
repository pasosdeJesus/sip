#!/bin/sh

if (test -f ".env") then {
  . .env
} fi;
f=`date +%Y-%m-%d`
fecharep=$f 
guion="lib/guiones/actualiza_osm.rb"
if (test "$RAILS_ENV" = "production") then {
	bin/railsp runner -e production $guion
} else {
	bin/rails runner -e development $guion
} fi;
