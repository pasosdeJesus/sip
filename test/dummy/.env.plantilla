export RC=sipd               # Arranque en modo producción en /etc/rc.d
export RAILS_ENV=development # Modo (development o production)
export IPDES=127.0.0.127     # Si corre en modo de desarrollo IP por usar
export PUERTODES=3000        # Si corre en modo de desarrollo puerto por usar
