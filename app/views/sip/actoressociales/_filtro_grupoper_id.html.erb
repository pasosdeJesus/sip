<%= f.input :grupoper_id,
  collection: Sip::Grupoper.where('id IN (SELECT grupoper_id FROM sip_actorsocial)'),
  label: false,
  include_blank: true,
  label_method: :presenta_nombre,
  value_method: :id,
  selected: Sip::ModeloHelper.poromision(params, :grupoper_id),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => ''
  }
%>
