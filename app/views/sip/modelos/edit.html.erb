<%- model_class = clase.constantize -%>
<div class="page-header">
  <% if !defined?(@formulario_sin_titulo) || !@formulario_sin_titulo %>
    <h1><%=t '.title', 
      :default => ["Edita #{model_class.human_attribute_name(
        model_class.model_name.to_s.demodulize)}"] 
      %>
    </h1>
  <% end %>
</div>

<%= render :partial => 'form' %>
