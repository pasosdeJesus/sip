<%= f.input :buspais,
  collection: Sip::Pais.habilitados,
  include_blank: true,
  label_method: :nombre,
  value_method: :id,
  label: false,
  selected: Sip::ModeloHelper.poromision(params, :buspais),
  input_html: {
    class: 'chosen-select',
    'data-enviarautomatico' => ''
  }
%>
